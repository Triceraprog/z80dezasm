$0000		[rom_start]
			Ce commentaire sera situé au-dessus puisqu'il est avec le label

$0004		%NTS

$0008		[chk_chr]
			F: Check Character
			I: HL, adresse du caractère à vérifier
			O: ...

$0008		Met dans A le caractère pointé par HL

$0009-$000c	L'adresse de retour présente sur la pile est prise pour
			l'adresse du deuxième argument...

$1000       %BASE(label)
            Un adresse sera exprimée par rapport à un label

$1010       %CHAR
            Un paramètre sera exprimé sous la forme d'un caractère
            Ça vaut peut-être le coup d'avoir une liste de paramètres sous forme
            de caractères par défaut et de spécifier %CHAR ou %NUM
            Ne le faire (comme pour les NTS) que si les caractères sont ASCII.
